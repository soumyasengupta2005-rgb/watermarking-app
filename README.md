A simple yet modern **Python GUI application** to add professional watermarks to images.  
Built with **Tkinter** and **Pillow (PIL)**, this tool provides a live preview, color picker, font size control, and multiple watermark positions — all in a clean, responsive interface.

---

## 🚀 Features

- **Upload Image** – Import `.png`, `.jpg`, `.jpeg`, `.bmp`, or `.gif` files.  
- **Custom Text Watermark** – Type any text to overlay on the image.  
- **Font Size Slider** – Adjust the watermark text size dynamically.  
- **Color Picker** – Choose any color for the watermark text.  
- **Position Options** – Place watermark in Top-Left, Top-Right, Center, Bottom-Left, or Bottom-Right.  
- **Live Preview** – Instantly view the image with your watermark applied.  
- **Reset Option** – Revert back to the original image anytime.  
- **Save Output** – Export your final image as `.png` or `.jpg`.  
- **Error Handling** – Gracefully handles user cancellations and invalid operations.

---

## Requirements

- Python 3.8+
- Pillow (`pip install pillow`)
- Tkinter (usually included with Python)

---

##  How to Run
1. Clone or download this repository.  
2. Install dependencies:
   ```bash
   pip install pillow

Run the app:
bash
Copy code
python main.py
Upload an image, customize your watermark, and save the result!

Project Structure
bash
Copy code
watermarking-app/
│
├── main.py            # Main application file
├── README.md          # Documentation
└── assets/ (optional) # For sample images or fonts

Future Improvements
Custom font selection
Support for image watermark overlays
Option to batch-process multiple images
Drag-and-drop image upload

Author
Soumya Sengupta
Physics Student & Python Developer
Exploring tools that merge science, design, and functionality.
